// Photo metadata type
export interface PhotoMetadata {
  filename: string
  title: string
  place: string
  date: string
  quote: string
}

// Existing types from the boilerplate can remain here
export interface WindowFrame {
  width: number
  height: number
}
